{
  "name": "shaderify",
  "version": "0.0.0",
  "description": "Shaderify is a transform for browserify which allow you to import glsl files by require.",
  "main": "./lib/shaderify.js",
  "keywords": [
    "browserify",
    "glsl"
  ],
  "dependencies": {
    "browserify-transform-tools": "~1.3.3"
  },
  "devDependencies": {
    "browserify": "^11.0.1",
    "chai": "^3.2.0",
    "coffee-script": "^1.8.0",
    "mocha": "^2.1.0"
  },
  "scripts": {
    "test": "mocha test",
    "prepublish": "coffee -c -o lib src && mocha test",
    "build": "coffee -c -o lib src"
  },
  "author": "pnlybubbles",
  "license": "MIT"
}
